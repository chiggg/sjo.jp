#!/usr/bin/perl
#
# Photo Gallery Movable Type Plugin
#
# $Id: $

use strict;
use lib "lib", ($ENV{MT_HOME} ? "$ENV{MT_HOME}/lib" : "../../lib");
use MT::Bootstrap App => 'PhotoGallery::XPWizard';

__END__

